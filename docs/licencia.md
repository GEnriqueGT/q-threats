# Licencia

El proyecto se distribuye bajo **GNU Affero General Public License v3.0**.

Consulta el archivo `LICENSE` en la **raíz del repositorio** (fuera de esta carpeta `docs/`).

Si modificas el código y ofreces el programa como **servicio en red**, AGPL exige que los usuarios puedan obtener el **código fuente** correspondiente a la versión que se ejecuta. Para el texto legal completo, lee la licencia.
